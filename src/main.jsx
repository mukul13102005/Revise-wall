import React from "react";
import ReactDOM from "react-dom/client";
import ReviseWall from "./ReviseWall.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ReviseWall />
  </React.StrictMode>
);
